{
	"name": "ursusapp",
	"version": "1.0.0",
	"description": "My Full Stack Application",
	"main": "server/app.js",
	"scripts": {
		"client-install": "cd client && npm install",
		"server-install": "cd server && npm install",
		"start": "npm run client-install && npm run server-install && concurrently \"npm run server\" \"npm run client\"",
		"server": "cd server && npm run dev",
		"client": "cd client && npm run serve"
	},
	"keywords": [],
	"author": "Your Name",
	"license": "ISC",
	"dependencies": {
		"@achrinza/node-ipc": "^10.1.10",
		"concurrently": "^8.2.2",
		"express": "^4.18.2",
		"mongoose": "^5.11.15"
	},
	"engines": {
		"node": ">=14.x"
	},
	"devDependencies": {
		"nodemon": "^3.0.1"
	}
}
